<Application x:Class="TransparentCalendar.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#F2181818"/>
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#40FFFFFF"/>

        <!--
            深色控件样式 —— 全部是**隐式样式**（无 x:Key），因此设置窗口、日期编辑器、
            笔记编辑器里的原生控件会自动套用，不必逐个挂 Style。
            主窗口那些带 x:Key 的样式（CalendarButtonStyle / NavButtonStyle /
            SidebarModeButtonStyle / OverlayTextBoxStyle）是显式指定的，优先级更高，不受影响。

            用中性白做强调色：应用里青/橙/琥珀/玫红都已经有语义了（放假/调休/待办/重要），
            控件状态再引入一个强调色会和它们打架。
        -->
        <Color x:Key="SurfaceColor">#1FFFFFFF</Color>
        <SolidColorBrush x:Key="CtrlFill" Color="#1AFFFFFF"/>
        <SolidColorBrush x:Key="CtrlFillHover" Color="#2BFFFFFF"/>
        <SolidColorBrush x:Key="CtrlFillPressed" Color="#3DFFFFFF"/>
        <SolidColorBrush x:Key="CtrlLine" Color="#33FFFFFF"/>
        <SolidColorBrush x:Key="CtrlLineHover" Color="#59FFFFFF"/>
        <SolidColorBrush x:Key="CtrlText" Color="#F0F2F5"/>
        <SolidColorBrush x:Key="CtrlTextDim" Color="#8A93A0"/>
        <SolidColorBrush x:Key="AccentFill" Color="#EDEFF2"/>
        <SolidColorBrush x:Key="AccentInk" Color="#14161A"/>
        <SolidColorBrush x:Key="PopupFill" Color="#F51C1F26"/>

        <!-- 按钮 -->
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource CtrlText}"/>
            <Setter Property="Background" Value="{StaticResource CtrlFill}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CtrlLine}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <!-- 注意：定宽的小按钮（如 30×30 的 ×）必须显式 Padding="0"，
                 否则内边距会把字形挤掉。 -->
            <Setter Property="Padding" Value="9,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource CtrlFillHover}"/>
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource CtrlLineHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource CtrlFillPressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 文本框 -->
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource CtrlText}"/>
            <Setter Property="CaretBrush" Value="{StaticResource CtrlText}"/>
            <Setter Property="SelectionBrush" Value="#66FFFFFF"/>
            <Setter Property="Background" Value="#14FFFFFF"/>
            <Setter Property="BorderBrush" Value="#26FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#1FFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#26FFFFFF"/>
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource CtrlLineHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 复选框：勾选态用中性白填充 + 深色对勾，不引入新的强调色 -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource CtrlText}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="Box"
                                    Width="18"
                                    Height="18"
                                    CornerRadius="5"
                                    Background="{StaticResource CtrlFill}"
                                    BorderBrush="{StaticResource CtrlLine}"
                                    BorderThickness="1"
                                    VerticalAlignment="Center"
                                    SnapsToDevicePixels="True">
                                <Path x:Name="Tick"
                                      Data="M 0,4 L 3.6,7.6 L 9.4,1"
                                      Stroke="{StaticResource AccentInk}"
                                      StrokeThickness="2"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="9,0,0,0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{StaticResource AccentFill}"/>
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentFill}"/>
                                <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource CtrlLineHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 下拉框条目 -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource CtrlText}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Row"
                                Background="Transparent"
                                CornerRadius="6"
                                Margin="4,1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Row" Property="Background" Value="{StaticResource CtrlFillHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Row" Property="Background" Value="{StaticResource CtrlFillPressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 下拉框 -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource CtrlText}"/>
            <Setter Property="Background" Value="{StaticResource CtrlFill}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CtrlLine}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="PART_Toggle"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Chrome"
                                                Background="{StaticResource CtrlFill}"
                                                BorderBrush="{StaticResource CtrlLine}"
                                                BorderThickness="1"
                                                CornerRadius="8">
                                            <Path x:Name="Arrow"
                                                  Data="M 0,0 L 4.5,4.5 L 9,0"
                                                  Stroke="{StaticResource CtrlText}"
                                                  StrokeThickness="1.6"
                                                  StrokeStartLineCap="Round"
                                                  StrokeEndLineCap="Round"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,11,0"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource CtrlFillHover}"/>
                                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource CtrlLineHover}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource CtrlFillPressed}"/>
                                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource CtrlLineHover}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="11,0,30,0"
                                              VerticalAlignment="Center"
                                              IsHitTestVisible="False"/>

                            <!-- 弹出层浮在桌面上方，底色必须接近不透明才看得清 -->
                            <Popup x:Name="PART_Popup"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   VerticalOffset="4"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="{StaticResource PopupFill}"
                                        BorderBrush="{StaticResource CtrlLineHover}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="16" ShadowDepth="3" Opacity="0.5" Color="Black"/>
                                    </Border.Effect>
                                    <ScrollViewer Margin="0,4">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 滑块：已选区间用中性白，未选区间压暗 -->
        <Style TargetType="Slider">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="IsSnapToTickEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center" Height="20" Background="Transparent">
                            <Border Height="4"
                                    CornerRadius="2"
                                    Background="{StaticResource CtrlFill}"
                                    VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge" Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4"
                                                        CornerRadius="2"
                                                        Background="{StaticResource AccentFill}"
                                                        VerticalAlignment="Center"
                                                        Opacity="0.85"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge" Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Grid Background="Transparent" Width="18" Height="18">
                                                    <Ellipse x:Name="Knob"
                                                             Width="13"
                                                             Height="13"
                                                             Fill="{StaticResource AccentFill}"
                                                             Stroke="#59000000"
                                                             StrokeThickness="0.5"/>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Knob" Property="Width" Value="16"/>
                                                        <Setter TargetName="Knob" Property="Height" Value="16"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="Knob" Property="Width" Value="16"/>
                                                        <Setter TargetName="Knob" Property="Height" Value="16"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 分组框：默认模板的凹槽边框在深色底上很脏 -->
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource CtrlTextDim}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <ContentPresenter ContentSource="Header"
                                              Margin="2,0,0,6"
                                              TextElement.FontWeight="SemiBold"/>
                            <Border Grid.Row="1"
                                    Background="#0FFFFFFF"
                                    BorderBrush="{StaticResource CtrlLine}"
                                    BorderThickness="1"
                                    CornerRadius="9"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
