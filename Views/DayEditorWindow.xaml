<Window x:Class="TransparentCalendar.Views.DayEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="每日记录"
        Width="420"
        Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Margin="8"
            Padding="18"
            CornerRadius="10"
            Background="#CC1A1A1A"
            BorderBrush="#55FFFFFF"
            BorderThickness="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,16" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Button Content="×"
                    Width="30"
                    Height="30"
                    Padding="0"
                    FontSize="16"
                    DockPanel.Dock="Right"
                    Click="Cancel_Click"/>
            <TextBlock x:Name="DateTitle"
                       FontSize="22"
                       Foreground="White"
                       FontWeight="Light"
                       VerticalAlignment="Center"/>
        </DockPanel>

        <GroupBox Grid.Row="1" Header="待办" Foreground="White" BorderBrush="#40FFFFFF" Margin="0,0,0,14">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ItemsControl x:Name="TodoItems">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <DockPanel Margin="0,4">
                                <Button Content="删除"
                                        Width="46"
                                        Padding="0"
                                        DockPanel.Dock="Right"
                                        Margin="8,0,0,0"
                                        Click="DeleteTodo_Click"/>
                                <Button Content="推迟"
                                        Width="46"
                                        Padding="0"
                                        DockPanel.Dock="Right"
                                        Margin="8,0,0,0"
                                        Click="PostponeTodo_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDone}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <ComboBox Width="64"
                                          Margin="8,0,0,0"
                                          DockPanel.Dock="Right"
                                          SelectedValue="{Binding Priority, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          SelectedValuePath="Content">
                                    <ComboBoxItem Content="普通"/>
                                    <ComboBoxItem Content="重要"/>
                                </ComboBox>
                                <CheckBox IsChecked="{Binding IsDone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          VerticalAlignment="Center"/>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             MinWidth="180"/>
                                    <TextBlock Text="{Binding PostponedLabel}"
                                               Foreground="#CCFFD166"
                                               FontSize="11"
                                               Margin="0,2,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PostponedLabel}" Value="">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Grid.Row="1"
                        Content="添加待办"
                        HorizontalAlignment="Left"
                        Margin="0,8,0,0"
                        Padding="10,4"
                        Click="AddTodo_Click"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="日记" Foreground="White" BorderBrush="#40FFFFFF" Margin="0,0,0,14">
            <TextBox x:Name="DiaryText"
                     Margin="8"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Background="#22FFFFFF"
                     Foreground="White"
                     BorderBrush="#40FFFFFF"/>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Width="76" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="保存" Width="76" Click="Save_Click"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>
